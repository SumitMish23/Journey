import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'journal.minimalistic',
  appName: 'journal-minimalistic',
  webDir: 'dist'
};

export default config;
